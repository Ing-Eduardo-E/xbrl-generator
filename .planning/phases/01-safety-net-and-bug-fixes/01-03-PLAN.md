---
phase: 01-safety-net-and-bug-fixes
plan: 03
type: execute
wave: 2
depends_on:
  - 01-02
files_modified:
  - app/src/lib/xbrl/official/excelRewriter.ts
autonomous: true
requirements:
  - BUG-01
  - R414-01
  - R414-02
  - R414-04

must_haves:
  truths:
    - "Running `pnpm test` shows all r414Pipeline.test.ts tests pass, including Hoja2!I15=40000000, J15=35000000, K15=25000000, and P15=100000000 with MOCK_ACCOUNTS that include isLeaf:false parent nodes"
    - "R414 Hoja2 (ESF) cells are populated with distributed service balances — not zeros"
    - "R414 Hoja3 (ER) cells are populated with income and expense values"
    - "R414 Hoja1 (general info) has company name, NIT, RUPS, and report date filled"
    - "The IFE pipeline tests added in Plan 01 continue to pass — no IFE regression from this change"
  artifacts:
    - path: "app/src/lib/xbrl/official/excelRewriter.ts"
      provides: "Fixed R414 leaf detection using codesWithChildren Set"
      contains: "codesWithChildren"
  key_links:
    - from: "excelRewriter.ts R414 section"
      to: "consolidatedAccounts loop"
      via: "codesWithChildren.has(account.code) replaces !account.isLeaf"
      pattern: "codesWithChildren\\.has\\(account\\.code\\)"
    - from: "excelRewriter.ts per-service section"
      to: "accountsByService loop"
      via: "codesWithChildren.has(account.code) replaces !account.isLeaf in service loops"
      pattern: "serviceCodesWithChildren\\.has\\(account\\.code\\)"
---

<objective>
Fix the R414 empty-data bug (BUG-01) by replacing the `!account.isLeaf` DB flag check with dynamic `codesWithChildren` Set detection in excelRewriter.ts.

Purpose: The DB-stored `isLeaf` flag may be incorrect for pre-aggregated balance sheets where parent accounts also appear as rows. Dynamic detection is the correct approach already used by BaseTemplateService. After this fix, R414 ESF, ER, and FC01 cells will be populated (R414-01, R414-02). R414-04 (Hoja1 company fields) is already implemented in the codebase — this plan verifies it works end-to-end via the existing r414Pipeline tests.

Output: Modified app/src/lib/xbrl/official/excelRewriter.ts
</objective>

<execution_context>
@C:/Users/rekin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rekin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@I:\Proyectos2025\xbrl-generator\.planning\ROADMAP.md
@I:\Proyectos2025\xbrl-generator\.planning\STATE.md

File to fix (R414 loops with isLeaf checks):
@I:\Proyectos2025\xbrl-generator\app\src\lib\xbrl\official\excelRewriter.ts

Reference implementation of codesWithChildren pattern:
@I:\Proyectos2025\xbrl-generator\app\src\lib\xbrl\shared\baseTemplateService.ts

Tests that must pass after fix:
@I:\Proyectos2025\xbrl-generator\app\src\lib\xbrl\__tests__\r414Pipeline.test.ts

<interfaces>
<!-- Fix pattern from baseTemplateService.ts lines 240-245 -->

// BUILD ONCE before the R414 section's first loop — uses ALL consolidated accounts:
const codesWithChildren = new Set<string>();
for (const account of options.consolidatedAccounts) {
  for (let i = 1; i < account.code.length; i++) {
    codesWithChildren.add(account.code.slice(0, i));
  }
}
// isLeaf = account.code NOT in codesWithChildren

// For per-service loops (accountsByService[service]) — these are a subset of
// consolidatedAccounts codes, so the same consolidated codesWithChildren Set applies.
// Build one additional set for service accounts to be consistent:
const serviceCodesWithChildren = new Set<string>();
for (const service of activeServices) {
  const svcAccounts = accountsByService[service] || [];
  for (const account of svcAccounts) {
    for (let i = 1; i < account.code.length; i++) {
      serviceCodesWithChildren.add(account.code.slice(0, i));
    }
  }
}

// REPLACE:  if (!account.isLeaf) continue;
// WITH:     if (codesWithChildren.has(account.code)) continue;   (for consolidated loops)
// WITH:     if (serviceCodesWithChildren.has(account.code)) continue; (for service loops)

Confirmed isLeaf locations to replace in excelRewriter.ts (from research):
  Line 201: Hoja2 consolidated loop (inside R414_ESF_MAPPINGS for loop)
  Line 222: Hoja2 per-service loop (inside accountsByService[service] for loop)
  Line 246: Hoja3 consolidated loop
  Line 274: Hoja3 per-service loop
  Line 290-291: Hoja3 debug block (suma51/suma52) — also uses !account.isLeaf
  Line 325: processSectionWithZeroFill inner loop (Hoja7 PPE section)
  (Also check the rest of processSectionWithZeroFill and any other R414 loops in Sections 3-5)

CRITICAL: Do NOT change anything in the 'ife' block (lines 1780+). Only change R414 sections.
CRITICAL: The 'grupo1/2/3' section calls rewriteGrupoData() — do not touch it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace isLeaf checks with codesWithChildren in R414 loops</name>
  <files>app/src/lib/xbrl/official/excelRewriter.ts</files>
  <behavior>
    - After fix: r414Pipeline.test.ts test "debe distribuir valores por servicio en columnas I,J,K de Hoja2" passes with I15=40000000, J15=35000000, K15=25000000
    - After fix: r414Pipeline.test.ts test "debe escribir datos ESF en Hoja2" passes with P15=100000000, P16=5000000
    - After fix: r414Pipeline.test.ts test "debe escribir datos ER en Hoja3" passes with L14 typeof 'number' and not 0
    - After fix: NEW test with MOCK_ACCOUNTS that include isLeaf:false parents (accounts code:'1', '11') still produces correct totals — confirms dynamic detection works
    - IFE pipeline tests from Plan 01 continue to pass (IFE section is unchanged)
    - templatePackage.test.ts from Plan 02 continues to pass
  </behavior>
  <action>
    Read excelRewriter.ts to find ALL locations of `if (!account.isLeaf) continue` in R414 sections.

    Step 1 — Find all isLeaf occurrences in R414 sections (between line 189 `if (options.niifGroup === 'r414')` and line 1770 `if (options.niifGroup === 'grupo1' || ...)`):
    Run: grep -n "account.isLeaf" I:\Proyectos2025\xbrl-generator\app\src\lib\xbrl\official\excelRewriter.ts

    Step 2 — After the `accountsByService` grouping block (around line 166) and BEFORE the `if (options.niifGroup === 'r414')` block, add two Set constructions:

    ```typescript
    // Dynamic leaf detection — replaces unreliable DB isLeaf flag
    // Pattern from baseTemplateService.ts lines 240-245
    const codesWithChildren = new Set<string>();
    for (const account of options.consolidatedAccounts) {
      for (let i = 1; i < account.code.length; i++) {
        codesWithChildren.add(account.code.slice(0, i));
      }
    }

    const serviceCodesWithChildren = new Set<string>();
    for (const service of activeServices) {
      const svcAccounts = accountsByService[service] || [];
      for (const account of svcAccounts) {
        for (let i = 1; i < account.code.length; i++) {
          serviceCodesWithChildren.add(account.code.slice(0, i));
        }
      }
    }
    ```

    Step 3 — Replace every `if (!account.isLeaf) continue;` inside R414 sections:
    - In consolidated loops (iterating over options.consolidatedAccounts or consolidatedAccounts): replace with `if (codesWithChildren.has(account.code)) continue;`
    - In per-service loops (iterating over accountsByService[service] or svcAccounts or serviceAccounts): replace with `if (serviceCodesWithChildren.has(account.code)) continue;`

    Step 4 — After making all replacements, confirm NO `account.isLeaf` references remain inside R414 sections. Check that the IFE section (starts at `if (options.niifGroup === 'ife')` around line 1780) is UNTOUCHED.

    Step 5 — Run tests to confirm GREEN:
    cd I:\Proyectos2025\xbrl-generator\app && pnpm test -- r414Pipeline --reporter=verbose

    If any test fails, read the failing assertion carefully. Common issue: the test checks for a specific numeric value (like P15=100000000) — if the mock data has isLeaf:false parents with non-zero values, those would now be INCLUDED (wrong). Check: the MOCK_ACCOUNTS in r414Pipeline.test.ts only have isLeaf:true accounts, so there are no parent accounts to accidentally include. The fix should not change results for MOCK_ACCOUNTS where all accounts already have isLeaf:true (codesWithChildren detection would yield same result).

    Step 6 — Run full test suite: pnpm test — confirm no regressions.
  </action>
  <verify>
    <automated>cd I:\Proyectos2025\xbrl-generator\app && pnpm test -- r414Pipeline --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    - All r414Pipeline.test.ts tests pass (including service column distribution test)
    - No `if (!account.isLeaf) continue` remains in R414 sections of excelRewriter.ts
    - `codesWithChildren` and `serviceCodesWithChildren` sets are built once before the R414 block
    - IFE pipeline tests still pass (Plan 01 tests not regressed)
    - templatePackage.test.ts tests still pass (Plan 02 tests not regressed)
    - `pnpm test` completes with no new failures
  </done>
</task>

</tasks>

<verification>
Run `cd I:\Proyectos2025\xbrl-generator\app && pnpm test 2>&1 | tail -20` — all tests must pass including r414Pipeline, ifePipeline (safety net), templatePackage.
</verification>

<success_criteria>
- BUG-01 fixed: dynamic codesWithChildren detection replaces DB isLeaf flag
- R414-01: Hoja2 (ESF) cells populated with service balances
- R414-02: Hoja3 (ER) cells populated with income/expense values
- R414-04: Hoja1 company fields verified passing via existing r414Pipeline tests (E12=companyName confirmed in "retornar solo metadatos" test)
- Zero test regressions across full suite
</success_criteria>

<output>
After completion, create I:\Proyectos2025\xbrl-generator\.planning\phases\01-safety-net-and-bug-fixes\01-03-SUMMARY.md
</output>
