---
phase: 01-safety-net-and-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/lib/xbrl/__tests__/ifePipeline.test.ts
autonomous: true
requirements:
  - TEST-01
  - BUG-02

must_haves:
  truths:
    - "Running `pnpm test` produces a passing ifePipeline.test.ts that writes known IFE accounts, reads back non-zero ESF cells from Hoja3, and verifies Hoja1 metadata"
    - "The test explicitly passes Q1 (reportDate 2025-03-31) and asserts the .xbrlt customized by customizeXbrlt() contains PrimerTrimestre — not SegundoTrimestre — in the XSD URL (proving BUG-02 exists)"
    - "All existing 302 tests continue to pass after adding the new test file"
  artifacts:
    - path: "app/src/lib/xbrl/__tests__/ifePipeline.test.ts"
      provides: "IFE E2E pipeline test + BUG-02 regression test"
      min_lines: 80
  key_links:
    - from: "ifePipeline.test.ts"
      to: "official/excelRewriter.ts (rewriteFinancialDataWithExcelJS)"
      via: "direct import, real template buffer, real ExcelJS workbook"
      pattern: "rewriteFinancialDataWithExcelJS.*niifGroup.*ife"
    - from: "ifePipeline.test.ts"
      to: "official/templateCustomizers.ts (customizeXbrlt)"
      via: "direct import, raw .xbrlt file content from disk"
      pattern: "customizeXbrlt.*niifGroup.*ife.*2025-03-31"
---

<objective>
Write the IFE pipeline safety-net test (TEST-01) and a failing BUG-02 regression test.

Purpose: Establish a test baseline before any bug fix is applied. The IFE test must pass today (IFE ESF writing is confirmed working via the ifeTemplateService singleton). The BUG-02 test for the XSD URL must FAIL today and PASS after Plan 04 applies the fix — that RED→GREEN cycle is the proof of correction.

Output: app/src/lib/xbrl/__tests__/ifePipeline.test.ts
</objective>

<execution_context>
@C:/Users/rekin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rekin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@I:\Proyectos2025\xbrl-generator\.planning\ROADMAP.md
@I:\Proyectos2025\xbrl-generator\.planning\STATE.md

Canonical test pattern to follow exactly:
@I:\Proyectos2025\xbrl-generator\app\src\lib\xbrl\__tests__\r414Pipeline.test.ts

File under test (IFE section starts at line 1780):
@I:\Proyectos2025\xbrl-generator\app\src\lib\xbrl\official\excelRewriter.ts

Bug location (IFE block returns at line 110 without XSD URL substitution):
@I:\Proyectos2025\xbrl-generator\app\src\lib\xbrl\official\templateCustomizers.ts

<interfaces>
<!-- Key cells confirmed from excelRewriter.ts IFE section lines 1797-1816 -->

IFE Hoja1 cell layout (DIFFERENT from R414 — do not copy R414 addresses):
  E13 = options.nit
  E14 = options.companyId
  E15 = options.companyName
  E16 = options.reportDate

IFE Hoja3 ESF service columns (confirmed lines 1784-1787):
  acueducto  → column I
  alcantarillado → column J
  aseo → column K
  (first data row is 15)

Template file paths (confirmed from XBRL_COMPATIBILITY.md):
  xlsx: app/public/templates/ife/IFE_SegundoTrimestre_ID20037_2025-06-30.xlsx
  xbrlt: app/public/templates/ife/IFE_SegundoTrimestre_ID20037_2025-06-30.xbrlt

customizeXbrlt signature (templateCustomizers.ts line 28):
  function customizeXbrlt(content: string, options: TemplateCustomization, outputFileName: string): string

TemplateWithDataOptions for IFE includes:
  niifGroup: 'ife'
  companyId: string
  companyName: string
  reportDate: string  (YYYY-MM-DD, e.g. '2025-03-31' for Q1)
  nit?: string
  consolidatedAccounts?: AccountData[]
  serviceBalances?: ServiceBalanceData[]
  activeServices?: string[]
  ifeCompanyData?: {
    address?: string; city?: string; phone?: string; email?: string;
    employeesStart?: number; employeesEnd?: number; employeesAverage?: number;
  }

AccountData type (from r414Pipeline.test.ts):
  { code: string; name: string; value: number; isLeaf: boolean; level: number; class: string }

ServiceBalanceData type:
  { service: string; code: string; name: string; value: number; isLeaf: boolean }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write ifePipeline.test.ts with pipeline test and BUG-02 regression</name>
  <files>app/src/lib/xbrl/__tests__/ifePipeline.test.ts</files>
  <behavior>
    - Test 1 (describe "IFE Pipeline E2E"): beforeAll loads IFE xlsx template from disk once. Test writes Q1 data (reportDate 2025-03-31), reads result with ExcelJS, asserts Hoja3 cell I15 is typeof 'number' and !== 0.
    - Test 2 (same describe): writes Q1 data, reads result, asserts Hoja1 cells: E13 is '800123456' (nit), E15 is 'Empresa de Prueba S.A. E.S.P.' (companyName). NOTE: E13=NIT, E14=companyId, E15=companyName — not R414 layout.
    - Test 3 (describe "IFE URL Bug — BUG-02"): loads raw .xbrlt template content from disk with readFile('utf-8'), calls customizeXbrlt() with niifGroup:'ife', reportDate:'2025-03-31', asserts result DOES NOT contain 'IFE_PuntoEntradaSegundoTrimestre-2025.xsd'. This test MUST FAIL before Plan 04 fix. Do not mark it .skip — it must be a real failing test.
    - Test 4 (same BUG-02 describe): same as Test 3 but for Q3 (reportDate '2025-09-30'), asserts result does not contain SegundoTrimestre XSD, asserts result contains 'IFE_PuntoEntradaTercerTrimestre-2025.xsd'. Also MUST FAIL before Plan 04.

    MOCK_ACCOUNTS: Reuse the same set as r414Pipeline.test.ts (copy the array). These accounts have isLeaf: true and cover PUC prefixes that IFE ESF mappings use.

    MOCK_SERVICE_BALANCES: Use same generateServiceBalances() helper (40% acueducto, 35% alcantarillado, 25% aseo).

    Do NOT import exceljs.mock.ts. Pipeline tests require a real workbook.
    Timeouts: 30000ms per test (same as r414Pipeline.test.ts).
    Use resolve(__dirname, '../../../../public/templates/ife/...') for template paths.
  </behavior>
  <action>
    Create the file app/src/lib/xbrl/__tests__/ifePipeline.test.ts following the r414Pipeline.test.ts structure exactly:

    1. Imports: vitest (describe, it, expect, beforeAll), rewriteFinancialDataWithExcelJS from '../official/excelRewriter', customizeXbrlt from '../official/templateCustomizers', ExcelJS from 'exceljs', readFile from 'fs/promises', resolve from 'path', AccountData and ServiceBalanceData from '../types'.

    2. MOCK_ACCOUNTS array: copy from r414Pipeline.test.ts (same 23 accounts, same isLeaf:true values). Add two parent-level accounts with isLeaf:false to verify the IFE section correctly handles them — add { code:'1', name:'Activos', value:0, isLeaf:false, level:1, class:'1' } and { code:'11', name:'Efectivo', value:0, isLeaf:false, level:2, class:'1' }. The IFE section uses the ifeTemplateService singleton which uses dynamic detection — so isLeaf:false parents should be excluded correctly.

    3. generateServiceBalances() helper: exact copy from r414Pipeline.test.ts.

    4. MOCK_SERVICE_BALANCES: const generated from helper.

    5. MOCK_IFE_OPTIONS constant (not inside describe so it can be reused):
       { niifGroup: 'ife' as const, companyId: '20037', companyName: 'Empresa de Prueba S.A. E.S.P.', reportDate: '2025-03-31', nit: '800123456', consolidatedAccounts: MOCK_ACCOUNTS, serviceBalances: MOCK_SERVICE_BALANCES, activeServices: ['acueducto', 'alcantarillado', 'aseo'], ifeCompanyData: { address: 'Calle 1 #2-3', city: 'Bogotá', phone: '6011234567', email: 'contacto@empresa.com.co', employeesStart: 50, employeesEnd: 52, employeesAverage: 51 } }

    6. describe('IFE Pipeline E2E') with:
       - beforeAll: loads IFE xlsx template, timeout 30000
       - it('debe escribir datos ESF en Hoja3 para Q1'): calls rewriteFinancialDataWithExcelJS with MOCK_IFE_OPTIONS, loads result into new ExcelJS.Workbook, asserts hoja3 defined, I15 typeof 'number' and not toBe(0)
       - it('debe escribir metadatos en Hoja1 para Q1'): same call, asserts E13 = '800123456', E15 = 'Empresa de Prueba S.A. E.S.P.'

    7. describe('IFE URL Bug — BUG-02') with:
       - beforeAll: loads raw .xbrlt content with readFile(path, 'utf-8'), timeout 30000
       - it('Q1 xbrlt NO debe referenciar SegundoTrimestre XSD [FAILS before BUG-02 fix]'): calls customizeXbrlt(content, { niifGroup:'ife', companyId:'20037', companyName:'Empresa de Prueba', reportDate:'2025-03-31' }, 'IFE_PrimerTrimestre_ID20037_2025-03-31'), asserts result does NOT contain 'IFE_PuntoEntradaSegundoTrimestre-2025.xsd'
       - it('Q3 xbrlt debe referenciar TercerTrimestre XSD [FAILS before BUG-02 fix]'): calls customizeXbrlt with reportDate:'2025-09-30' and outputFileName 'IFE_TercerTrimestre_ID20037_2025-09-30', asserts result does NOT contain SegundoTrimestre, asserts result DOES contain 'IFE_PuntoEntradaTercerTrimestre-2025.xsd'

    After writing the file, run `cd I:\Proyectos2025\xbrl-generator\app && pnpm test -- ifePipeline` and check output:
    - Tests in "IFE Pipeline E2E" describe must PASS (IFE ESF writing is confirmed working)
    - Tests in "IFE URL Bug" describe must FAIL (BUG-02 not yet fixed)
    - If "IFE Pipeline E2E" tests fail, check that IFE section in excelRewriter.ts uses ifeTemplateService singleton (lines 1780+) rather than inline isLeaf checks — if inline, those tests may legitimately fail (document in summary)

    Do NOT use the exceljs.mock.ts mock. Do NOT call workbook.addWorksheet() in any test setup.
  </action>
  <verify>
    <automated>cd I:\Proyectos2025\xbrl-generator\app && pnpm test -- ifePipeline --reporter=verbose 2>&1 | tail -40</automated>
  </verify>
  <done>
    - ifePipeline.test.ts exists in app/src/lib/xbrl/__tests__/
    - "IFE Pipeline E2E" tests pass (ESF cells non-zero, Hoja1 metadata correct)
    - "IFE URL Bug — BUG-02" tests fail with assertion errors (not import/syntax errors)
    - All previously passing tests still pass: `pnpm test` shows no regressions in r414Pipeline, baseTemplateService, etc.
  </done>
</task>

</tasks>

<verification>
Run `cd I:\Proyectos2025\xbrl-generator\app && pnpm test 2>&1 | tail -20` to confirm total test count increased by the new tests and no prior tests regressed.
</verification>

<success_criteria>
- ifePipeline.test.ts exists with pipeline + BUG-02 regression tests
- IFE pipeline tests pass (safety net established for IFE)
- BUG-02 tests fail as expected (RED state confirmed before fix)
- Total passing test count does not decrease from baseline
</success_criteria>

<output>
After completion, create I:\Proyectos2025\xbrl-generator\.planning\phases\01-safety-net-and-bug-fixes\01-01-SUMMARY.md
</output>
