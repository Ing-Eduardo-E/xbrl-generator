---
phase: 01-safety-net-and-bug-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/lib/xbrl/__tests__/templatePackage.test.ts
autonomous: true
requirements:
  - TEST-02
  - TEST-03

must_haves:
  truths:
    - "Running `pnpm test` shows a passing templatePackage.test.ts that verifies generateOfficialTemplatePackageWithData() returns an OfficialTemplatePackage whose base64 fileData decodes to a ZIP containing exactly 5 entries (.xbrlt, .xml, .xlsx, .xbrl, README.txt)"
    - "The .xlsx entry in the ZIP is loadable by ExcelJS without throwing"
    - "All existing 302+ tests continue to pass after adding the new test file"
  artifacts:
    - path: "app/src/lib/xbrl/__tests__/templatePackage.test.ts"
      provides: "ZIP shape tests for generateOfficialTemplatePackageWithData()"
      min_lines: 60
  key_links:
    - from: "templatePackage.test.ts"
      to: "officialTemplateService.ts (generateOfficialTemplatePackageWithData)"
      via: "direct import, real template files from disk"
      pattern: "generateOfficialTemplatePackageWithData.*niifGroup.*r414"
    - from: "templatePackage.test.ts"
      to: "JSZip.loadAsync()"
      via: "decode base64 → Buffer, then JSZip inspection"
      pattern: "JSZip.loadAsync.*zipBuffer"
---

<objective>
Write the ZIP shape tests for generateOfficialTemplatePackageWithData() (TEST-02 and TEST-03).

Purpose: Establish a regression baseline proving the ZIP assembly pipeline works — 5 files present, correct extensions, xlsx loadable by ExcelJS. This provides the safety net for preserveOriginalStructure() which currently has zero test coverage.

Output: app/src/lib/xbrl/__tests__/templatePackage.test.ts
</objective>

<execution_context>
@C:/Users/rekin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rekin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@I:\Proyectos2025\xbrl-generator\.planning\ROADMAP.md
@I:\Proyectos2025\xbrl-generator\.planning\STATE.md

Canonical test pattern:
@I:\Proyectos2025\xbrl-generator\app\src\lib\xbrl\__tests__\r414Pipeline.test.ts

Function under test and its return type:
@I:\Proyectos2025\xbrl-generator\app\src\lib\xbrl\officialTemplateService.ts

<interfaces>
<!-- From officialTemplateService.ts lines 135-205 -->

generateOfficialTemplatePackageWithData(options: TemplateWithDataOptions): Promise<OfficialTemplatePackage>

OfficialTemplatePackage shape:
  { fileName: string; fileData: string; mimeType: string }
  (fileData is base64-encoded ZIP — must decode with Buffer.from(pkg.fileData, 'base64') before JSZip.loadAsync)

ZIP contents (5 files, not 4 — research doc said 4 but code also adds README.txt):
  - {outputFileName}.xbrlt
  - {outputFileName}.xml
  - {outputFileName}.xlsx
  - {outputFileName}.xbrl
  - README.txt

outputFileName pattern: '{outputPrefix}_ID{companyId}_{reportDate}'
For r414: outputPrefix = 'R414Ind', companyId = '20037', reportDate = '2024-12-31'
→ files named 'R414Ind_ID20037_2024-12-31.xbrlt' etc.

JSZip API (already in project dependencies):
  import JSZip from 'jszip';
  const zip = await JSZip.loadAsync(zipBuffer);
  const fileNames = Object.keys(zip.files);
  const xlsxBuffer = await zip.files[xlsxEntry].async('nodebuffer');

ExcelJS load (for xlsx validation):
  const wb = new ExcelJS.Workbook();
  await wb.xlsx.load(xlsxBuffer);  // must not throw

MOCK_ACCOUNTS and MOCK_SERVICE_BALANCES: copy same arrays from r414Pipeline.test.ts
  (same 23 accounts, all isLeaf: true)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write templatePackage.test.ts with ZIP shape tests</name>
  <files>app/src/lib/xbrl/__tests__/templatePackage.test.ts</files>
  <behavior>
    - Test 1 (TEST-03): ZIP must contain exactly 5 files with correct extensions: one .xbrlt, one .xml, one .xlsx, one .xbrl, one README.txt. Assertion: fileNames.some(f => f.endsWith('.xbrlt')) toBe(true), same for .xml, .xlsx, .xbrl, and fileNames.includes('README.txt').
    - Test 2 (TEST-02): The .xlsx entry in the ZIP must be loadable by ExcelJS without throwing. Extract .xlsx buffer from ZIP, call wb.xlsx.load(buffer) and expect it resolves without error.
    - Test 3 (TEST-02 data): The ExcelJS-loaded xlsx must have a worksheet named 'Hoja1'. This confirms preserveOriginalStructure() preserved the sheet names.
    - Test 4 (TEST-03): fileName in OfficialTemplatePackage must end with '.zip' and match pattern 'R414Ind_ID20037_2024-12-31.zip'. mimeType must be 'application/zip'.
  </behavior>
  <action>
    Create app/src/lib/xbrl/__tests__/templatePackage.test.ts.

    1. Imports: vitest (describe, it, expect, beforeAll), generateOfficialTemplatePackageWithData from '../officialTemplateService', JSZip from 'jszip', ExcelJS from 'exceljs', AccountData and ServiceBalanceData from '../types'.

    2. MOCK_ACCOUNTS: copy the 23-account array from r414Pipeline.test.ts (all isLeaf: true).

    3. generateServiceBalances() and MOCK_SERVICE_BALANCES: copy helper from r414Pipeline.test.ts.

    4. describe('generateOfficialTemplatePackageWithData — ZIP Shape (TEST-02, TEST-03)') with:
       - beforeAll: call generateOfficialTemplatePackageWithData with { niifGroup: 'r414', companyId: '20037', companyName: 'Empresa de Prueba S.A. E.S.P.', reportDate: '2024-12-31', consolidatedAccounts: MOCK_ACCOUNTS, serviceBalances: MOCK_SERVICE_BALANCES, activeServices: ['acueducto', 'alcantarillado', 'aseo'] }, store result as pkg. Decode pkg.fileData from base64 to Buffer. Load into JSZip. Store zip and fileNames as suite-level variables. Timeout: 30000ms.
       - it('ZIP must contain .xbrlt file'): expect(fileNames.some(f => f.endsWith('.xbrlt'))).toBe(true)
       - it('ZIP must contain .xml file'): expect(fileNames.some(f => f.endsWith('.xml'))).toBe(true)
       - it('ZIP must contain .xlsx file'): expect(fileNames.some(f => f.endsWith('.xlsx'))).toBe(true)
       - it('ZIP must contain .xbrl file'): expect(fileNames.some(f => f.endsWith('.xbrl'))).toBe(true)
       - it('ZIP must contain README.txt'): expect(fileNames).toContain('README.txt')
       - it('xlsx must be loadable by ExcelJS without error'): extract xlsx entry, get nodebuffer, create new ExcelJS.Workbook, await wb.xlsx.load(xlsxBuffer as any), no throw
       - it('xlsx must contain Hoja1 worksheet'): load xlsx with ExcelJS (fresh workbook), call wb.getWorksheet('Hoja1'), expect it toBeDefined()
       - it('package fileName must match expected pattern'): expect(pkg.fileName).toBe('R414Ind_ID20037_2024-12-31.zip')
       - it('package mimeType must be application/zip'): expect(pkg.mimeType).toBe('application/zip')

    5. Do NOT use exceljs.mock.ts. Do NOT call workbook.addWorksheet() in test helpers.

    After writing, run `cd I:\Proyectos2025\xbrl-generator\app && pnpm test -- templatePackage --reporter=verbose 2>&1 | tail -40`. All tests in this file must pass.
  </action>
  <verify>
    <automated>cd I:\Proyectos2025\xbrl-generator\app && pnpm test -- templatePackage --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    - templatePackage.test.ts exists in app/src/lib/xbrl/__tests__/
    - All 8 tests in the file pass
    - Total test count in `pnpm test` increases (no regressions to existing tests)
  </done>
</task>

</tasks>

<verification>
Run `cd I:\Proyectos2025\xbrl-generator\app && pnpm test 2>&1 | tail -20` to confirm all tests pass (templatePackage tests added, no regressions).
</verification>

<success_criteria>
- templatePackage.test.ts exists with 8 passing tests
- ZIP shape is verified (5 files, correct types, xlsx loadable)
- preserveOriginalStructure() now has a regression test baseline
- Total passing test count does not decrease from baseline
</success_criteria>

<output>
After completion, create I:\Proyectos2025\xbrl-generator\.planning\phases\01-safety-net-and-bug-fixes\01-02-SUMMARY.md
</output>
