---
phase: 01-safety-net-and-bug-fixes
plan: 04
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - app/src/lib/xbrl/official/templateCustomizers.ts
autonomous: true
requirements:
  - BUG-02
  - IFE-01

must_haves:
  truths:
    - "The BUG-02 tests added in Plan 01 now PASS: Q1 xbrlt does not contain SegundoTrimestre XSD, Q3 xbrlt contains TercerTrimestre XSD"
    - "customizeXbrlt() with reportDate '2025-03-31' produces a .xbrlt whose content contains 'IFE_PuntoEntradaPrimerTrimestre-2025.xsd'"
    - "customizeXbrlt() with reportDate '2025-09-30' produces a .xbrlt whose content contains 'IFE_PuntoEntradaTercerTrimestre-2025.xsd'"
    - "customizeXbrlt() with reportDate '2025-12-31' produces a .xbrlt whose content contains 'IFE_PuntoEntradaCuartoTrimestre-2025.xsd'"
    - "Q2 (reportDate '2025-06-30') continues to work correctly — SegundoTrimestre XSD is preserved for Q2"
    - "All previously passing tests continue to pass — no regressions"
  artifacts:
    - path: "app/src/lib/xbrl/official/templateCustomizers.ts"
      provides: "IFE trimestre-aware XSD entry-point URL substitution"
      contains: "IFE_PuntoEntrada"
  key_links:
    - from: "templateCustomizers.ts IFE block"
      to: "XSD entry-point URL replacement"
      via: "regex replace before return statement at line 110"
      pattern: "IFE_PuntoEntrada.*Trimestre.*\\.xsd"
---

<objective>
Fix the IFE URL bug (BUG-02) by adding XSD entry-point URL substitution to the IFE block in templateCustomizers.ts before the return statement.

Purpose: For Q1, Q3, and Q4, the generated .xbrlt file currently contains the wrong taxonomy entry-point XSD URL (SegundoTrimestre). XBRL Express uses this URL to locate the correct taxonomy schema. Without the correct URL, Q1/Q3/Q4 packages are rejected. The fix is a single regex replacement added before `return customized` at the end of the IFE block.

Output: Modified app/src/lib/xbrl/official/templateCustomizers.ts
</objective>

<execution_context>
@C:/Users/rekin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rekin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@I:\Proyectos2025\xbrl-generator\.planning\ROADMAP.md
@I:\Proyectos2025\xbrl-generator\.planning\STATE.md

File to fix (IFE block ends at line 110-111 with return customized):
@I:\Proyectos2025\xbrl-generator\app\src\lib\xbrl\official\templateCustomizers.ts

BUG-02 regression tests that must turn GREEN after this fix:
@I:\Proyectos2025\xbrl-generator\app\src\lib\xbrl\__tests__\ifePipeline.test.ts

<interfaces>
<!-- Fix location: templateCustomizers.ts lines 57-111 (IFE block) -->

Current IFE block structure (simplified):
  if (options.niifGroup === 'ife') {
    // ... date replacements (lines 60-108) ...
    customized = customized.replace(/<startDate>2025-04-01...  (and 3 more date replaces)
    return customized;   // ← BUG: returns WITHOUT replacing XSD entry-point URL
  }

Quarter name mapping to use (matches SSPD XSD filename conventions exactly):
  '03' → 'PrimerTrimestre'
  '06' → 'SegundoTrimestre'
  '09' → 'TercerTrimestre'
  '12' → 'CuartoTrimestre'

reportMonth is already computed at line 61:
  const reportMonth = options.reportDate.split('-')[1];

The template .xbrlt file contains this exact string for Q2:
  IFE_PuntoEntradaSegundoTrimestre-2025.xsd

Regex to replace it (captures the year to preserve it):
  customized = customized.replace(
    /IFE_PuntoEntrada\w+Trimestre-(\d{4})\.xsd/g,
    `IFE_PuntoEntrada${trimName}-$1.xsd`
  );

Where trimName comes from the mapping above using reportMonth.

IMPORTANT: For Q2 (reportMonth === '06'), trimName = 'SegundoTrimestre' — the replacement
is a no-op (same string). This means Q2 behavior is preserved exactly.

Also check: does the .xbrlt contain the template's output filename with SegundoTrimestre?
Example: 'IFE_SegundoTrimestre_ID20037_2025-06-30.xlsx' in the config= attribute.
The config= replacement at lines 46-52 already handles the filename substitution via
outputFileName parameter. Verify that it covers this case. The XSD URL is separate from
the config= attribute — the regex above targets only the XSD entry-point reference.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add XSD entry-point URL substitution to IFE block in templateCustomizers.ts</name>
  <files>app/src/lib/xbrl/official/templateCustomizers.ts</files>
  <behavior>
    - Before fix: customizeXbrlt() with Q1 returns string containing 'IFE_PuntoEntradaSegundoTrimestre-2025.xsd' (wrong)
    - After fix: customizeXbrlt() with Q1 returns string containing 'IFE_PuntoEntradaPrimerTrimestre-2025.xsd' (correct)
    - After fix: Q2 returns string containing 'IFE_PuntoEntradaSegundoTrimestre-2025.xsd' (unchanged — regression check)
    - After fix: Q3 returns string containing 'IFE_PuntoEntradaTercerTrimestre-2025.xsd'
    - After fix: Q4 returns string containing 'IFE_PuntoEntradaCuartoTrimestre-2025.xsd'
  </behavior>
  <action>
    Read templateCustomizers.ts lines 57-111 to understand the exact structure of the IFE block.

    The fix adds these lines IMMEDIATELY BEFORE `return customized;` inside the `if (options.niifGroup === 'ife')` block:

    ```typescript
    // Reemplazar URL del entry-point XSD según el trimestre
    // BUG-02: El template tiene siempre SegundoTrimestre — debe reemplazarse para Q1/Q3/Q4
    const trimNamesXsd: Record<string, string> = {
      '03': 'PrimerTrimestre',
      '06': 'SegundoTrimestre',
      '09': 'TercerTrimestre',
      '12': 'CuartoTrimestre',
    };
    const trimName = trimNamesXsd[reportMonth] ?? 'SegundoTrimestre';
    customized = customized.replace(
      /IFE_PuntoEntrada\w+Trimestre-(\d{4})\.xsd/g,
      `IFE_PuntoEntrada${trimName}-$1.xsd`
    );
    ```

    This is a minimal, targeted change — only 10 lines added before the existing `return customized;`. No other lines are modified.

    After editing the file, run the BUG-02 tests:
    cd I:\Proyectos2025\xbrl-generator\app && pnpm test -- ifePipeline --reporter=verbose

    The "IFE URL Bug — BUG-02" tests that were FAILING in Plan 01 must now PASS.
    The "IFE Pipeline E2E" tests must continue to PASS.

    Then run the full suite to check for regressions:
    cd I:\Proyectos2025\xbrl-generator\app && pnpm test 2>&1 | tail -20
  </action>
  <verify>
    <automated>cd I:\Proyectos2025\xbrl-generator\app && pnpm test -- ifePipeline --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    - templateCustomizers.ts IFE block now replaces the XSD entry-point URL before returning
    - BUG-02 regression tests in ifePipeline.test.ts now PASS (GREEN — they were RED in Plan 01)
    - Q2 continues to produce SegundoTrimestre XSD (no regression)
    - All other tests in full suite still pass
  </done>
</task>

</tasks>

<verification>
Run `cd I:\Proyectos2025\xbrl-generator\app && pnpm test 2>&1 | tail -20` — all tests must pass. Count of passing tests should be higher than the baseline (new BUG-02 tests now pass that were failing before).
</verification>

<success_criteria>
- BUG-02 fixed: IFE XSD entry-point URL updated for all 4 trimestres
- IFE-01: IFE can now pass XBRL Express validation for Q1, Q3, Q4 (URL was the blocker)
- ifePipeline.test.ts "IFE URL Bug" tests: all PASS
- Zero test regressions across full suite
- Phase 1 complete: all 9 requirement IDs addressed (TEST-01, TEST-02, TEST-03, BUG-01, BUG-02, R414-01, R414-02, R414-04, IFE-01)
</success_criteria>

<output>
After completion, create I:\Proyectos2025\xbrl-generator\.planning\phases\01-safety-net-and-bug-fixes\01-04-SUMMARY.md
</output>
